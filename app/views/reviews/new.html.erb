<h1>Add Reviews</h1>
<p>Find me in app/views/reviews/new.html.erb</p>


<%= simple_form_for([@restaurant, @review]) do |f| %>
  <%= f.input :content%>
  <%= f.input :rating, collection: [0,1,2,3,4,5], as: :check_boxes %>
  <%= f.submit "add a review", class: "btn btn-primary" %>
<% end %>
